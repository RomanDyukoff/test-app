import { Panel } from '../../components/Panel/Panel';
import './style.css';

export const PromoPage = () => {
    return (
        <section className="promo-page">
            <Panel />
        </section>
    )
}
